1. Create AWS Instance

2. SSH connect

```
ssh -i "minsky-aws.pem" ubuntu@ec2-43-205-118-245.ap-south-1.compute.amazonaws.com
```

- [ ] AWS instance basic upgrade, node setup

```bash
sudo apt update
sudo apt upgrade -y
```

[Node](https://nodejs.org/en/download)

- [ ] Code trasfer

- [ ] Node some change
