import type { Field } from "payload";

export const scrollLabel: Field = {
  name: "scrollLabel",
  type: "textarea",
  localized: true,
  defaultValue:
    "Section content which goes in the scroll spy element, lets make it long just like this so that we can stress test the ui. See how long i made it. But still its not long enough. maybe we never will be long enough, but we cant stop trying because of that. After all being human is being long",
};
